## Project rules

- **Never auto-commit.** Only stage and commit files when the user explicitly says "提交代码" or "commit". Do not commit after completing a task unless asked.
- **Commit messages must follow** `docs/COMMIT_CONVENTION.md` (Angular convention adapted for this project). Every message must use `<type>(<scope>): <subject>` format with proper body/footer when applicable.
